# A simple function definition follows.
# The def keyword must be followed by 
# a function name (identifier) that respects
# the variable naming rules.
def summ(array):
    summ = 0
    for elem in array:
        summ += elem
    return summ
