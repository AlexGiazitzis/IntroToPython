password = "Strong password!"

# An if statement as a one-liner, knows as a conditional expression, commonly called a ternary expression
print("Access denied.") if password != "Strong password" else print("Access granted.")
