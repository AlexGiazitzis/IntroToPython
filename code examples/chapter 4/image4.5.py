# from example of Image 4.1
intList = [1, 2, 3, 4, 5]

# Using a for loop, it is possible to index
# into the list and manipulate it element-wise
for i in range(0, len(intList)):
    print(intList[i])
