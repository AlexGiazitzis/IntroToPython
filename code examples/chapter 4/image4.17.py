# Similarly, sorting in the reverse direction
# can be perfored with the sort() method
# but with the reverse optional parameter
# being specified as True
li = [1, 3, 8, 0, 10, 123, 5]
li.sort(reverse=True)
print(li)
