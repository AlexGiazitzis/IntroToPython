# from example of Image 4.1
intList = [1, 2, 3, 4, 5]

# It is possible to iterate over a list without indexing
# using, in a what is called in other languages as, an enhanced loop
for elem in intList:
    print(elem)
